DATABASE_URL=postgresql://USER@localhost:5432/adscore_dev
SESSION_SECRET=openssl-rand-base64-32-ciktisi
META_APP_ID=
META_APP_SECRET=
# Token şifreleme anahtarı (AES-256-GCM): openssl rand -base64 32
META_TOKEN_KEY=
# Opsiyonel: Facebook Login for Business configuration ID (App Dashboard'da oluşturulur).
# Boşsa klasik scope listesiyle OAuth açılır.
META_LOGIN_CONFIG_ID=
# Webhook doğrulaması için kendi belirlediğin token (App Dashboard'a da aynısı girilir).
META_WEBHOOK_VERIFY_TOKEN=
GEMINI_API_KEY=
GEMINI_MODEL=
GEMINI_IMAGE_MODEL=
GROQ_API_KEY=
